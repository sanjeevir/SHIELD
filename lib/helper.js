"use strict()";

/* encode all domain names in the URLs found in the document to hipenated names. */
var encodeURLsInDocument = function (doc) {

};

/* replace all hiphens in the domain name of the URL string given to dots and give back the actual domain name. */
var decodeURL = function (url) {

};

/* return a list of URLs found in the document string */
var extractURLsFromString = function (doc) {

};